<h2><%= @task.name %></h2>
<p><%= @task.details%></p>


<p>
   <%= link_to 'back to list', tasks_path, class: 'btn btn-light'%>
</p>

<p>
   <%= link_to 'edit task', edit_task_path, class: 'btn btn-light'%>
</p>


 <p>
    <%= link_to '🗑', task_path(@task), class: 'btn btn-light', data: { turbo_method: :delete, turbo_confirm: "Delete #{@task.name}?" } %>
  </p>
